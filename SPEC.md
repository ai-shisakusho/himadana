# Prototype 004 仕様書 — 暇なにする？（仮）

## 1. コンセプト

暇だけど何をするか決められず、結局スマホを眺めて時間を使ってしまうときに、
**「やりたいのに後回しにしていること」を1つ選び、そのまま行動開始まで促すアプリ。**

単なるランダム抽選ではなく、

**決める → 少し急かされる → すぐ始める → 完了する**

までを一連の体験にする。

アプリの人格は「嫌味ではなく、ちょっと笑えるお節介なやつ」。

---

## 2. Prototype情報

- Prototype: 004
- Version: 0.2
- 仮称: 暇なにする？
- 対象: スマートフォン優先
- 公開想定: GitHub Pages
- 費用: 0円
- ログイン: なし
- サーバー: なし
- DB: なし
- 外部API: なし

---

## 3. 技術構成

- HTML
- CSS
- JavaScript
- localStorage

外部ライブラリは使用しない。

---

## 4. 初回起動

localStorageに行動データがない場合のみ、サンプル行動を登録する。

ユーザーは初回から設定不要で使える。

### 初期サンプル

| 行動 | 時間 |
|---|---:|
| ストレッチする | 5分 |
| 机の上を片付ける | 5分 |
| 筋トレする | 10分 |
| 気になっていたことを1つ調べる | 10分 |
| 読書する | 15分 |
| 勉強する | 15分 |
| 散歩する | 20分 |
| 部屋を片付ける | 20分 |
| 映画・ドラマを1話見る | 30分 |
| 趣味の時間にする | 30分 |

---

## 5. 基本フロー

1. ホームで暇な時間を選ぶ（5 / 15 / 30 / 60分）
2. 「何する？」を押す
3. 選択時間以内、かつ有効な行動から1件ランダム提案
4. 10秒カウントダウン開始
5. 「これにする！」または「別のを出す」
6. 「これにする！」で行動の設定時間を使ってタイマー開始
7. 「できた！」で完了回数と今日の完了数を加算
8. 完了画面から「まだ暇？ もう一回」でホームへ戻れる

---

## 6. 行動抽選

- 選択した暇時間以下の行動のみ対象
- OFFの行動は除外
- 候補が複数ある場合はランダム
- 可能な限り直前に表示した行動は連続表示しない
- 候補が1件しかない場合は同じ行動の再表示を許可

候補が0件なら、行動追加への導線を表示する。

---

## 7. 10秒カウントダウンと人格

メッセージは行動内容に紐付けず、**全行動共通プール**とする。

理由: 文言は「行動」に語りかけるのではなく「暇なユーザー」に語りかけるため。

### フェーズ1: 10〜7秒

軽く促す。背景も穏やか。

例:
- まあ、ちょっとだけやってみる？
- 暇なら1個くらいやっとこ。
- とりあえず、これでどう？

### フェーズ2: 6〜4秒

少し踏み込む。

例:
- 考えてる間に始めた方が早い。
- あとでやる、って何回目だっけ。
- そろそろ決めてもいい頃です。

### フェーズ3: 3〜1秒

少し強め。ただし嫌味にしない。

例:
- そろそろ腹をくくろう。
- 別のを探しても、たぶん似たようなもんです。
- 1個だけ。始めれば勝ち。

10秒経過後も強制決定しない。

画面はフェーズに応じて背景色とCSSキャラクターの表情を変える。

---

## 8. 「別のを出す」ツッコミ

スキップ回数はlocalStorageへ保存せず、1提案サイクル内だけ保持する。

- 0〜2回: 表示なし
- 3回目: 固定「そろそろ1個選んでみない？ 👀」
- 4回目: 軽い文言をランダム表示
- 5回目: 固定「もしかして、やる気あるやつ探してない？」
- 6回目以降: 4回目系の軽い文言をランダム表示し、さらに煽らない

ツッコミは「別のを出す」ボタン付近へテキストのみ表示し、揺れなどの強いモーションは付けない。

「これにする！」確定時 / タイマー開始時にスキップ回数を0へ戻す。

---

## 9. タイマー

- 行動のminutesを初期時間として使用
- 一時停止
- 再開
- できた！
- 中止

通常動作中は終了予定時刻を基準に残時間を計算し、ブラウザタブが非表示になっても大きくずれない設計とする。

一時停止中は残時間を保持する。

タイマーが0になったら完了を促すが、完了回数への加算は「できた！」操作時に行う。

---

## 10. 完了記録

各行動にcompletedCountを保持する。

また、今日の達成数として以下をlocalStorageへ保存する。

```json
{
  "date": "YYYY-MM-DD",
  "count": 2
}
```

日付はユーザー端末のローカル日付基準。

日付比較は以下で行う。

- ホーム表示時
- 「できた！」処理時

保存済み日付と現在日付が異なる場合はtodayCountを0へリセットして現在日付を保存する。

完了画面では以下を表示する。

- 行動名
- 累計実行回数
- 今日何個目か
- 「まだ暇？ もう一回」

---

## 11. 行動管理

各行動は以下を持つ。

- id
- name
- minutes
- enabled
- completedCount

ユーザー操作:

- 新規追加
- 編集
- 削除
- ON / OFF
- サンプル行動へ戻す

「サンプル行動へ戻す」は現在の行動をすべて置き換えるため、確認を挟む。

---

## 12. 画面構成

### ホーム
- アプリ名
- 今日の達成数
- 5 / 15 / 30 / 60分
- 「何する？」
- 行動管理への導線

### 提案
- CSSキャラクター
- 行動名
- 所要時間
- 10秒カウントダウン
- フェーズ別メッセージ
- スキップツッコミ
- 「これにする！」
- 「別のを出す」

### タイマー
- 行動名
- 大きな残時間
- 一時停止 / 再開
- できた！
- 中止

### 完了
- できた表示
- 累計回数
- 今日の完了数
- 「まだ暇？ もう一回」

### 行動管理
- 行動一覧
- ON / OFF
- 編集
- 削除
- 追加
- サンプルへ戻す

---

## 13. UI方針

- スマートフォン優先
- 片手で押しやすい大きなボタン
- カード型
- 説明を増やしすぎない
- 「暇だ → 何する？ → 開始」を短くする
- 楽しいがゲーム化しすぎない
- アニメーションは必要最低限

---

## 14. セキュリティ・プライバシー

- データは端末内のみ
- 外部送信なし
- 個人情報取得なし
- 位置情報取得なし
- APIキーなし
- ユーザー入力はtextContent等の安全なDOM APIで表示
- ユーザー入力をinnerHTMLへ直接渡さない
- 外部ライブラリ/CDNは原則使用しない

---

## 15. Ver.0.2で実装しないもの

- 言い訳ブロック / 今日の調子入力
- 今日のスキップ傾向保存
- AI提案
- ログイン
- クラウド同期
- SNS共有
- 通知
- ランキング
- レベル / 経験値
- 詳細グラフ
- キャラクター育成
- 高度なおすすめロジック

これらはVer.0.3以降の候補とする。

---

## 16. 完成条件

- 初回サンプルあり
- 行動追加 / 編集 / 削除 / ON-OFF
- 5 / 15 / 30 / 60分から暇時間を選べる
- 選択時間以内の行動だけ抽選
- 直前候補を可能な限り避ける
- 10秒カウントダウン
- 3段階の背景 / キャラクター / 文言変化
- スキップ3 / 4 / 5回目以降のツッコミ
- 「これにする！」からタイマー開始
- 一時停止 / 再開 / 中止
- 「できた！」で累計 / 今日の完了数を保存
- 「まだ暇？ もう一回」
- 再読み込み後も設定保持
- サンプルへ戻せる
- スマホ表示が崩れない
- ユーザー入力から任意HTML / JavaScriptが実行されない

---

## 17. 公開後に見るポイント

1. 暇なとき本当に開くか
2. 提案後、そのまま行動を始めるか
3. 10秒演出が背中押しとして効くか
4. 「別のを出す」を何回押すか
5. スキップツッコミが面白いか / うざいか
6. サンプルを自分用に変更するか
7. 完了後に「もう一回」を使うか

最重要指標は、**提案された後に実際にタイマー開始まで進むか**。
